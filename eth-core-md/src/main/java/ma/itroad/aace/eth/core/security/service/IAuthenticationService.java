/*
package ma.itroad.aace.eth.core.security.service;

import ma.itroad.aace.eth.core.security.bean.Credentials;
import ma.itroad.aace.eth.core.security.bean.JWTToken;
import ma.itroad.aace.eth.core.security.bean.UserDetailsAuthBean;
import org.springframework.http.ResponseEntity;
import org.springframework.security.core.userdetails.UserDetailsService;
import org.springframework.security.core.userdetails.UsernameNotFoundException;

*/
/**
 * 
 * @author A. Jbili Base interface which loads Authenticated user-specific data.
 *//*

public interface IAuthenticationService extends UserDetailsService {
	
	boolean isAValidCredentials(Credentials credentials);

	UserDetailsAuthBean loadUserByUsername(String username) throws UsernameNotFoundException;
	
	JWTToken authorize(Credentials credentials) throws Exception;
	
	ResponseEntity<JWTToken> authenticate(Credentials validatedCredentials) throws Exception;
}
*/
